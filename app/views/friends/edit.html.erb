<% @page_title = "名前の編集" %>
<h2><%= @page_title %></h2>

<%= form_for @friend do |form| %>
  <%= render "form", form: form %>
  <div class="toolbar"><%= form.submit "更新" %></div>
<% end %>

<%= link_to "友達一覧に戻る", :friends %>